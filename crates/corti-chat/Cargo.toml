[package]
name = "corti-chat"
description = "Runtime-free hosted chat harness for Corti: the live rewrite/question coordinator, batching, fences, and cache policy."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
corti-postprocess = { workspace = true }
corti-postprocess-providers = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
# Keyed request digests for the coordinator's exact-cache ordering (same pin as the app crate).
ring = "0.17"
thiserror = { workspace = true }
tracing = { workspace = true }
unicode-segmentation = { workspace = true }
