{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "live-transcript-window",
  "description": "Timestamped live call transcript and ephemeral microphone/transcription test window.",
  "platforms": ["macOS"],
  "windows": ["live"],
  "permissions": [
    "core:default",
    "core:event:deny-emit",
    "core:event:deny-emit-to"
  ]
}
