<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Codesign entitlements (referenced by bundle.macOS.entitlements). Both keys describe audio input:
  `audio-input` is the current key, `microphone` the legacy one — include both for compatibility
  (design/05-app-tauri.md). These only take effect under the App Sandbox; for a Developer-ID (non-App-Store)
  build, mic/audio access is governed by TCC + the Info.plist usage strings, so we do NOT enable
  com.apple.security.app-sandbox here. Add it only when targeting the Mac App Store.
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.device.microphone</key>
    <true/>
</dict>
</plist>
